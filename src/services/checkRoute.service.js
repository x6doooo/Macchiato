/**
 * Created by dx.yang on 15/4/10.
 */


angular.module('macchiato')
    .service('checkRouteService', [
        '$location',
        function(
            $location
        ) {
            var self = this;

            self.get = function() {};
        }
    ]);
